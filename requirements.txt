# SparkIDE runtime dependencies.
# Install with:  pip install -r requirements.txt
# (Development/test tooling lives in requirements-dev.txt.)

# Core UI framework
PyQt6

# Provides QWebEngineView (to embed Blockly HTML) and QWebChannel (Python <-> JS bridge)
PyQt6-WebEngine

# Serial monitor: read Serial.print() output from the board
pyserial

# Realtime serial plotter widget
pyqtgraph
